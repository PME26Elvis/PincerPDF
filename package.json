{
  "name": "pincerpdf",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic browser verification for the PincerPDF application shell.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
